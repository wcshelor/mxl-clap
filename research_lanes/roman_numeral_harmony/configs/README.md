# Configs

This directory holds YAML configuration files for the experimental Roman numeral harmony lane.

- `rn_harmony_default.yml`: the default lightweight backend configuration.
- `rnbert_experimental.yml`: optional backend configuration for a future RNBert-style analyzer.
