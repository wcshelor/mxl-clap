# Cache

This directory stores inspectable intermediate Roman numeral event tables.

Each run should create its own subdirectory:

- `cache/<run_id>/events/<score_id>__rn_events.csv`
- `cache/<run_id>/run_manifest.json`
