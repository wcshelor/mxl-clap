# external

Clone the separate MXL toolbox into `external/mxl-toolbox/` if you want to use it locally.

This repo does not assume the toolbox is present. The first implementation uses a `music21` fallback for parsing and inspection.
