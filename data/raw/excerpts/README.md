# raw excerpts

Generated excerpt files land in `generated/`.

Keep the inputs small. This repo is a scaffold, not a dataset store.

If you want to inspect hand-picked excerpts directly, you can also place them here, but the excerpting pipeline writes its outputs to `generated/`.
